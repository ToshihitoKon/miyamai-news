# frozen_string_literal: true

# lib/ の実行に必要な gem（miyamai_news.rb の bundler/inline ブロックと同じ内容）に
# 加えて、テスト実行(RSpec)用の gem を宣言する。miyamai_news.rb 自体は bundler/inline
# のままなので、エンドユーザーは bundle install 無しで単体実行できる。
source "https://rubygems.org"

gem "rspec", "~> 3.13"
gem "tty-spinner"
gem "rss"
gem "csv"
gem "rexml"
gem "dry-struct"
gem "dry-types"
